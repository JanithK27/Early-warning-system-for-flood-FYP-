// Optional: Add functionality to handle form submission or navigation

document.querySelector("form").addEventListener("submit", function (event) {
    event.preventDefault();
    alert("Sign-up successful!");
});
